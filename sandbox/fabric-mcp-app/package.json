{
  "name": "fabric-mcp-app",
  "version": "1.0.0",
  "description": "Fabric Workspace Refresh Card — MCP App with interactive UI",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && tsc -p tsconfig.server.json && cross-env INPUT=mcp-app.html vite build && cross-env INPUT=workspace-provisioner.html vite build && cross-env INPUT=pipeline-status.html vite build && cross-env INPUT=empty-workspaces.html vite build && cross-env INPUT=single-admin-workspaces.html vite build",
    "start": "concurrently \"cross-env NODE_ENV=development INPUT=mcp-app.html vite build --watch\" \"tsx watch main.ts\"",
    "start:stdio": "node dist/main.js --stdio",
    "start:http": "node dist/main.js"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.0.1",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "concurrently": "^9.0.0",
    "cross-env": "^7.0.3",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0",
    "zod": "^3.23.0"
  }
}
